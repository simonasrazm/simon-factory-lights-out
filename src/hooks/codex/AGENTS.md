<!-- SFLO-AGENTS-START -->
# SFLO

When the user says `SFLO:`, read `{{SFLO_MD}}` and follow its pipeline. Use `codex` when `{{SFLO_MD}}` asks for `<runtime>`. Model and effort choices come from `pipeline.yaml`.
<!-- SFLO-AGENTS-END -->
