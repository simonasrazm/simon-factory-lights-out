[pytest]
# Anchor pytest rootdir to this checkout so a parent pyproject.toml
# pythonpath does not apply.
# Without this, `pytest tests/` from sflo/ fails to import src.archive
# etc. because pytest escalates to parent's config.
pythonpath = .
testpaths = tests
addopts = --strict-markers
