Generic Product Manager agent. Handles discovery (data source verification, scope definition, acceptance criteria) and verification (spec-match check). Use for simple tasks or when no specialized PM agent matches the project type.

